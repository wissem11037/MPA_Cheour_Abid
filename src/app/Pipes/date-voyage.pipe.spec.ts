import { DateVoyagePipe } from './date-voyage.pipe';

describe('DateVoyagePipe', () => {
  it('create an instance', () => {
    const pipe = new DateVoyagePipe();
    expect(pipe).toBeTruthy();
  });
});
